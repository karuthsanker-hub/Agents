# Railway Configuration for Arctic Debate Card Agent
# Author: Shiv Sanker

[build]
builder = "dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
